<?xml version="1.0" encoding="utf-8"?>
<!--
  Template consumed by the `packageMsix` Gradle task (see build.gradle.kts), which fills in the
  {{...}} tokens below from gradle.properties (msixPackageIdentityName/msixPublisher/
  msixPublisherDisplayName) and the app's version, then packs the result with makeappx.exe.

  This declares the app as a "Desktop Bridge" package (Windows.FullTrustApplication + the
  runFullTrust capability), i.e. the existing jpackage app-image running unmodified inside an
  MSIX wrapper - not a rewrite to native UWP/WinUI.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity
    Name="{{IDENTITY_NAME}}"
    Publisher="{{PUBLISHER}}"
    Version="{{VERSION}}" />

  <Properties>
    <DisplayName>sbs3Dfullscreen</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY_NAME}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>

  <Applications>
    <Application Id="sbs3Dfullscreen" Executable="{{EXECUTABLE}}" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="sbs3Dfullscreen"
        Description="Side-by-side 3D JPEG viewer"
        BackgroundColor="black"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
      </uap:VisualElements>
    </Application>
  </Applications>
</Package>
